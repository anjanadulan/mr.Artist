// AdBlock detector neutralized to comply with Google AdSense Publisher & Better Ads Standards
// Crawlers and visitors are never blocked or trapped in dead-end screens.
export function initAdBlockDetector() {
    // Non-blocking, passive compliance mode
    return;
}

